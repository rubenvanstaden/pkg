# Packages

Package install scripts from source

* Install user packages in:

```shell
# Source directory
~/.local/src

# Binary directory
~/.local/bin
```

* Install root packages in:

```shell
# Source directory
/usr/local/src

# Binary directory
/usr/local/bin
```
